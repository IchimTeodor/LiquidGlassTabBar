.DS_Store
DerivedData/
build/
build18/
build-tests/
.swiftpm/
.build/

# The demo's generated .xcodeproj is committed so the repo opens without
# XcodeGen, but per-user Xcode state inside it never is.
xcuserdata/
